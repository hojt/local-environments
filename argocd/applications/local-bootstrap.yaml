apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: local-bootstrap
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/hojt/local-environments.git
    targetRevision: main
    path: clusters/local/bootstrap

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
