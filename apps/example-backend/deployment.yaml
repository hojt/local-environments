apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-backend
  labels:
    app.kubernetes.io/name: example-backend
    app.kubernetes.io/part-of: local-environment
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: example-backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: example-backend
        app.kubernetes.io/part-of: local-environment
    spec:
      containers:
        - name: example-backend
          image: localhost:5001/example-backend:0.2.0
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
